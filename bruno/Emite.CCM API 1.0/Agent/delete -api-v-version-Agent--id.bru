meta {
  name: delete -api-v-version-Agent--id
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/v{{version}}/Agent/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}
