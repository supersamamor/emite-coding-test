meta {
  name: patch -api-v-version-Call--id
  type: http
  seq: 6
}

patch {
  url: {{baseUrl}}/api/v{{version}}/Call/:id
  body: none
  auth: bearer
}

params:query {
  ~agentId: 
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}
