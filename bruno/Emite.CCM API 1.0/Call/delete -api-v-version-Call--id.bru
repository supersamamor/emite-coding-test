meta {
  name: delete -api-v-version-Call--id
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/v{{version}}/Call/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}
