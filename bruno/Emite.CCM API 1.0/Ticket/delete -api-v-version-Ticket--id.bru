meta {
  name: delete -api-v-version-Ticket--id
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/v{{version}}/Ticket/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}
