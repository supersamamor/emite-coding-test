meta {
  name: put -api-v-version-Ticket--id
  type: http
  seq: 4
}

put {
  url: {{baseUrl}}/api/v{{version}}/Ticket/:id
  body: json
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}
